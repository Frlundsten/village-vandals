# Task 4 — Display +X/hr production rates in Home.vue header

Under each resource badge (food, wood, bricks, iron), add a small `+X/hr` sub-label bound to the
new store refs (`foodPerHour`, etc.). Style as small muted text beneath the resource count.

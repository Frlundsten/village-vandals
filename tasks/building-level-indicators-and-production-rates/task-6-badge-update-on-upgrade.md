# Task 6 — Update badge on building upgrade

In `handleUpgrade()` in `VillageNew.vue`:
- After refreshing `buildingsBySiteId`, look up the badge Text for the upgraded constructionSiteId.
- Update its text to the new level from the refreshed building data.

Write a Vitest test verifying badge text updates after an upgrade.

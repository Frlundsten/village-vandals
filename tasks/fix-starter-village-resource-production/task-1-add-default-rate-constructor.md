# Task 1 — Add default-rate constructor to ResourceProduction

## Goal
Add a `ResourceProduction(int defaultRate)` constructor that sets all four rates
(`woodPerHour`, `bricksPerHour`, `ironPerHour`, `foodPerHour`) to the given value.
Add a `withDefaults()` factory method that uses `DEFAULT_ECONOMICAL_PRODUCTION_RATE`.

## Files changed
- `src/main/java/com/villagevandals/vandals/resource/ResourceProduction.java`

## Status: DONE

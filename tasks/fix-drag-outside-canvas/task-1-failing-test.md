# Task 1 — Write failing test

Add a Vitest test to VillageNew.spec.js asserting that globalpointermove does NOT
move the container when no pointerdown on the canvas preceded it (dragInitiatedOnCanvas = false).
Must fail before the fix.

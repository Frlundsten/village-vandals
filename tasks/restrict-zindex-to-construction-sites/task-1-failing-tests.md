# Task 1 — Write failing tests

1. Update the existing "setupSprite zIndex" test to assert a plain tile has zIndex 0 (not row+col).
2. Add a test asserting a construction site tile sprite gets zIndex = row + col.
Both must fail before the implementation change.

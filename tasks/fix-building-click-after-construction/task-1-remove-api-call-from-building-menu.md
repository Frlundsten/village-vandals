# Task 1: Remove constructBuilding call from BuildingMenu

## What
- Remove the `constructBuilding` import from `BuildingMenu.vue`.
- Make `sendInfo` a plain (non-async) function that only emits `buildingType` and `closeMenu`.

## Acceptance Criteria
- AC3 (partial): `BuildingMenu` no longer owns the API call or its error handling.

## Status
DONE — implemented before spec was written.

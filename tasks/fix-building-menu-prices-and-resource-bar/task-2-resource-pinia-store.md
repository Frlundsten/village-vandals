# Task 2 — Create useResourceStore Pinia store

## Goal
Add `frontend/src/stores/resources.js` with a `useResourceStore` that holds
`food`, `wood`, `bricks`, `iron` state and exposes a `refresh(villageId)` action
that calls `refreshStorage` and updates the store.

## Files changed
- `frontend/src/stores/resources.js` — new

## Status: TODO

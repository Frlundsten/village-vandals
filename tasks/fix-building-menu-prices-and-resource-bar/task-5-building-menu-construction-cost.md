# Task 5 — Update BuildingMenu and BuildingPresentationCard to use constructionCost

## Goal
Change `BuildingMenu.vue` to pass `building.constructionCost` (not `building.upgradeCost`)
to `BuildingPresentationCard`. Update `BuildingPresentationCard.vue` prop name from
`upgradeCost` to `constructionCost` and update all internal references.

## Files changed
- `frontend/src/components/BuildingMenu.vue`
- `frontend/src/components/BuildingPresentationCard.vue`

## Status: TODO

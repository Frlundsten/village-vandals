# Task 3 — Update Home.vue to use useResourceStore

## Goal
Replace the local `resources` ref in `Home.vue` with `useResourceStore`.
Call `store.refresh(villageId)` instead of `refreshStorage` directly.
Template binds to store state instead of local ref.

## Files changed
- `frontend/src/views/Home.vue`

## Status: TODO
